@model IEnumerable<Specility>







<div class="row">
    <div class="col">
        @if (TempData["Message"] != null)
        {
            <div class="alert alert-info alert-dismissible fade show" role="alert">
                @TempData["Message"]
            </div>
        }
    </div>
</div>


<a class="btn btn-success left-po" asp-action="Create"> Create New </a>

<table class="table table-bordered border-primary">
    <thead>
        <tr>
            <th> # </th>
            <th>Specility Name</th>

        </tr>
    </thead>
    <tbody>
        @{
            int srno = 1;
        }
        @foreach (var temp in Model)
        {
            <tr>
                <th scope="row">@srno</th>
                <td>@temp.SpecilityName</td>

                <td>
                    <a asp-action="Edit" asp-route-id="@temp.SpecilityID" class="btn btn-primary"> Edit </a> |
                    <a asp-action="Delete" asp-route-id="@temp.SpecilityID" class="btn btn-danger"> Delete </a>

                </td>
            </tr>
            srno++;
        }
    </tbody>
</table>

